#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = Array.new(9) { ' ' }
puts 'Welcome to Tic Tac Toe!'
puts 'Where would you like to go?'
position = gets.strip

move(board, position)
display_board(board)