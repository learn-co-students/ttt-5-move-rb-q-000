#!/usr/bin/env ruby

require_relative '../lib/move.rb'

puts "Welcome to Tic Tac Toe!"
board = Array.new(9, " ")
puts "Where would you like to go?"
input = gets.strip
move(board, position = input, char = "X")
display_board(board)
# Code your CLI Here