#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
move(board, 0)
input_to_index(user_input = 0)

puts "Welcome to Tic Tac Toe!"
display_board(board)

puts "Where would you like to go?"
display_board(board)

go = gets.strip
display_board(board)

#!/usr/bin/env ruby -wKU
