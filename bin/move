#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"
position = gets.strip
position = .to_i
move(board, 0, "X")
move(board, 5, "O")
move(board, 9, "X")
move(board, 0, "O")